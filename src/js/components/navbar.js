/**
 * Navbar controller — handled entirely in main.js now.
 * This module is kept for backward-compat import.
 */
export function initNavbar() {
  // All navbar logic is in main.js → initNavbar()
  // Kept as empty export so old imports don't break.
}
